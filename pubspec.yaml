name: gg_golden
description: gg_golden implements a way to manage relational data in JSON.
version: 3.2.0
repository: https://github.com/ggsuite/gg_golden.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  collection: ^1.19.1
  matcher: ^0.12.20
  path: ^1.9.1

dev_dependencies:
  dna_ggsuite: ^0.2.0
  helix: ^1.3.0
  lints: ^6.1.0
  test: ^1.31.2